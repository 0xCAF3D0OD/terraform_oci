# Source from https://registry.terraform.io/providers/oracle/oci/latest/docs/resources/identity_compartment

# resource "oci_identity_compartment" "root_compartment" {
#     compartment_id = var.root_compartment_ocid
#     description = "compartment identity oci"
#     name = "${var.team}-${var.environment}"
# }